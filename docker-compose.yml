name: ipify

services:
  ipify:
    build: .
    image: ipify-api:local
    container_name: ipify
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:3000"
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"
